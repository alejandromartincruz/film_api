<?php

namespace Bundle\ActorBundle\Infrastructure\Repository;

use Bundle\ActorBundle\Domain\Entity\Actor;
use Doctrine\ORM\EntityManagerInterface;

/**
 * ActorRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class ActorRepository extends \Doctrine\ORM\EntityRepository implements \Bundle\ActorBundle\Domain\Interfaces\ActorRepository
{

    private $entityManager;

    public function __construct(EntityManagerInterface $entityManager)
    {
        $this->entityManager = $entityManager;
    }

    public function findAllOrderedByName()
    {
        return $this->entityManager
            ->createQuery(
                'SELECT a FROM ActorBundle:Actor a ORDER BY a.name ASC'
            )
            ->getResult();
    }

    public function findOneById(int $id): ?Actor
    {
        return $this->entityManager
            ->createQuery(
                'SELECT a FROM ActorBundle:Actor a WHERE a.id = :id'
            )
            ->setParameter('id', $id)
            ->getOneOrNullResult();
    }

    public function save(Actor $actor): void
    {
        $this->entityManager->persist($actor);
        $this->entityManager->flush();
    }

    public function delete(Actor $actor): void
    {
        $this->entityManager->remove($actor);
        $this->entityManager->flush();
    }
}
